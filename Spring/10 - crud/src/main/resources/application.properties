# Consola de h2

# spring.h2.console.enabled=true
# spring.datasource.url=jdbc:h2:mem:clientesdb
# spring.datasource.username=julian
# spring.datasource.password=sa
# spring.datasource.driver-class-name=org.h2.Driver

# Spring boot configura la bd de h2 en memeoria
# nombre de la bd = testdb
# usuario = sa (sin password) 

# Conectando a MySQL

spring.datasource.url=jdbc:mysql://localhost/springboot_db?serverTimezone=UTC-3&useSSL=false
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# crear tablas de forma automatica: (luego se eliminan)
# en produccion en realidad se crean antes

spring.jpa.hibernate.ddl-auto=create-drop
logging.level.org.hibernate.SQL=debug